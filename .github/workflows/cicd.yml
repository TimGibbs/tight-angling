name: CICD

on: push

env:
  NODE_VERSION: 16.x
  CACHE_DIR: 'npm'

jobs:
  test:
    uses: ./.github/workflows/test.yml

  build-deploy:
    if: github.ref == 'refs/heads/main'
    uses: ./.github/workflows/build-deploy.yml